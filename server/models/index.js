const User = require('./User');
const Tournament = require('./Tournament');
const Team = require('./Tournament');
const Match = require('./Tournament');

module.exports = {
    User,
    Tournament,
    Team,
    Match
}